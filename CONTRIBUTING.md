# Contributing

Run `pnpm check` before opening a change. Keep this repository code-only:
do not commit third-party game assets, character models, renderer SDK source,
runtime binaries or extracted application content.
